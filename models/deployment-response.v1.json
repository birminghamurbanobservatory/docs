{
  "title": "deployment-response",
  "type": "object",
  "x-tags": [
    "deployments"
  ],
  "properties": {
    "@context": {
      "type": "array",
      "items": {
        "type": "string",
        "format": "uri"
      }
    },
    "@id": {
      "type": "string",
      "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
      "maxLength": 44,
      "minLength": 1
    },
    "@type": {
      "type": "string"
    },
    "public": {
      "type": "boolean"
    },
    "description": {
      "type": "string"
    },
    "name": {
      "type": "string",
      "minLength": 1,
      "maxLength": 40
    },
    "createdAt": {
      "type": "string",
      "format": "date-time"
    },
    "updatedAt": {
      "type": "string",
      "format": "date-time"
    }
  },
  "required": [
    "@id",
    "public",
    "description",
    "name",
    "createdAt",
    "updatedAt",
    "@type",
    "@context"
  ]
}