{
  "title": "deployment-in-collection",
  "type": "object",
  "x-tags": [
    "deployments"
  ],
  "properties": {
    "@id": {
      "type": "string",
      "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
      "maxLength": 44,
      "minLength": 1
    },
    "@type": {
      "type": "string"
    },
    "name": {
      "type": "string",
      "minLength": 1,
      "maxLength": 40
    },
    "description": {
      "type": "string"
    },
    "public": {
      "type": "boolean"
    },
    "updatedAt": {
      "type": "string",
      "format": "date-time"
    },
    "createdAt": {
      "type": "string",
      "format": "date-time"
    }
  },
  "required": [
    "updatedAt",
    "createdAt",
    "name",
    "description",
    "public",
    "@id"
  ]
}