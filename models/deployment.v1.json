{
  "title": "deployment",
  "type": "object",
  "x-tags": [
    "deployments"
  ],
  "properties": {
    "@id": {
      "type": "string",
      "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$",
      "maxLength": 44,
      "minLength": 1
    },
    "public": {
      "type": "boolean"
    },
    "description": {
      "type": "string"
    },
    "name": {
      "type": "string",
      "minLength": 1,
      "maxLength": 40
    },
    "createdAt": {
      "type": "string",
      "format": "date-time"
    },
    "updatedAt": {
      "type": "string",
      "format": "date-time"
    }
  }
}